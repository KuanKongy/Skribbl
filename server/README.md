# Skribbl server

The authoritative game server for the Skribbl clone.

Full documentation — architecture, game flow, the socket protocol reference,
and testing — lives in the [root README](../README.md).

Quick start:

```sh
npm install
npm run dev    # nodemon on :3001 (PORT env to override)
npm test       # turn-engine unit tests
```
